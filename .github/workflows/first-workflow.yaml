name: firstWorkflows
on: [push]


jobs:
  first-action-job:
    runs-on: ubuntu:latest
    steps:
      - name: print command
        run: echo "Hello World!"
      - name: print command
        run: |
          node -v
          npm -v